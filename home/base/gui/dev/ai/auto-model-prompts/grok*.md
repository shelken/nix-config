## 额外优先规则(当前最高优先)

- 对于存在的文件不管变化有多大使用`edit`而不是`write`!!!!!!!!!!!!!不准对存在的文件使用write
- 禁止执行任何mac系统内置的系统命令 例如 `tccutil` / `networksetup` 等
- 使用 `git checkout`/`git reset` 命令前必须获得用户允许
- 任何项目外的删除操作应该和用户进行确认再做
- 使用`edit`更新任何文件而不是使用`bash/shell`; 
- 跟用户对话时,必须使用通俗易懂的中文描述,不准使用奇怪的英文还不解释
- 禁止对用户进行任何政策说教
