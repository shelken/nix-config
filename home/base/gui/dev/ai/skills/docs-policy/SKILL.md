---
name: docs-policy
description: 编写、修改项目文档、需要统一文档约定、审查的提交中包含文档 时阅读该技能
---

# 文档

## 基本

- **能够通过脚本/cli 实时动态且快速获取 的信息, 不要在文档进行记录, 而是在文档使用一条命令告诉如何获取, 这通常适用于那种经常变化的信息, 防止文档因为信息过时而腐败; 每次写出来的脚本, 务必自己验证进行确认和预期输出一致; 脚本实现前, 遵循ponytail原则, 其中复用脚本很重要, 找出已有脚本, 并思考如何复用, 避免创建更多散乱的脚本**
- 如果文档很重要, 检查AGENTS.md中是否引用文档, 如果没有,应该在`## 目录`引用文档并说明在什么场景下阅读; 如果来源于同一个目录的文档超过三个, 那么聚合他们, 并在目录下创建README.md进行所有, 在AGENTS.md中进行聚合成一条, 并聚合阅读的场景和简单介绍
- 当你要写新文档前, 先项目范围内搜索相关关键词, 寻找合适目录|寻找已存在的历史文档, 不要造成多文档重复
- 能够在代码中体现的信息, 也不要在文档进行记录, 而是指引去哪个代码文件名进行检查最新逻辑
- 当用户说保存plans时,通常将计划保存在`~/Code/docs/{project-name}/plans`下,名称格式`y-M-D-slug.md`
- 当项目没有对应 GitHub 仓库且需要保存 spec 时，保存到 `~/Code/docs/{project-name}/specs`，名称格式为 `y-M-D-slug.md`
- 文档记录问题(踩坑点和解决方式)时, 永远使用这个格式: `简短的说问题 -> 简短的目前最佳解决方式`
- 禁止在项目的任何文档中出现绝对路径, 使用 slug/变量 指代前缀
- 文档中切勿提及草稿、版本、评审轮次、先前表述、被替代的决策或编辑过程，除非用户明确要求记录变更日志、历史记录或决策记录或者文档属于自带记录变更的类型。(例如: 在文档中不写`第xx版`/`xx优化版本`)
- 所有面向Agent(而不是用户或开发者)写的文档, 都应该遵循这个原则: `文档应该记录的不是大量证据和枚举(因为会过时), 而是应该记录: 事实和证据是什么, 从哪获取的, 怎么获取(复现)的(如果有脚本或命令,直接引用)`
- 日志型文档如果需要被推翻证据, 应该使用`删除线`来进行旧版更新, 并新增新的证据记录

## 格式偏好

- 无须列表的末尾永远不写标点符号; 文档中一段话的末尾默认不写句号
- 所有中文文档的单/双引号使用`『』和「」`而不是`“ ”和‘ ’`
- 禁止破折号, 用逗号/句号/冒号代替
- 不使用形如`不是X,而是Y`/`很直接`/`很具体`等这种蹩脚的英文直译表达
- 当你需要引用多条命令时, 使用"```"包裹, 如果在无须列表项中,使用缩进放入对应的无须列表项中,例如
  ```md
  - {事实和证据是什么, 从哪获取的, 怎么获取}
    ```bash
    whoami
    ```
  ```

## 复现脚本的原则

- 必须足够高效(最短路径)且确定(幂等)性(多次运行不变)
- 能够合并与增强则优先修改旧的脚本(参考`ponytail`原则)
